# Prospective authority prerequisite: specification decision

Decision: **ACCEPT only the TP v1.20 prospective-authority amendment.** Codex exercises the user's delegated planning and independent review authority; this is not a panel decision or a new direct user approval. The effective set becomes shared1.15 + intent1.10 + TP1.20. The approved file-preview and all earlier accepted contracts remain effective. Implementation is not yet accepted.

The separately discussed D13 correction supplies the actual staged successor and drafted witness to the shared matrix row through stagedIndex. A kind string with the pre-state index could supply neither. Claude confirmed the exact source lookup, withdrew the redundant successorKind option, and agreed to one shared index-taking row. Newly consumed typed rulings are derived by the actual newlyConsumedRulingRefs(preIndex.store,U), resolved in the staged index and checked for packet freshness against preIndex. Candidate relationships are explicit; typed collision failures remain the shared global-id failures.

Codex checked the existing matrix, structure, references, governance, routing, packet and consumption implementations directly. The accepted extraction also preserves the existing interleaving of global id claims and object validation in persisted validateStructure, as required by unchanged diagnostic precedence. The staged pass claims all raw pre-state sections and candidate objects before indexing. It runs no incomplete future lifecycle validation and changes no writer mint permission.

D13 completed actualexit0,6turns,214.985seconds in the real local claude-opus-5/xhigh session. Its166-path read-only guard passed2026-09-06T07:59:37Z unchanged. The preceding lower request correction was independently accepted on78 fixed files:1756tests/1744pass/12skip, structure/eval7, lower22, remaining17, public28, preview25, writer109, consumer144, integration96 and E1 41, all actualexit0 with matching manifests and original bytes. Those are baseline implementation results, not results for this new helper.

The prerequisite is separable from unresolved controller branches. It accepts no epoch unlock, per-finding/task/fingerprint/evidence coverage, retained-payload recovery, owned verification or ledger-loop behavior. A successful pure authority check does not grant budget by itself. The one public helper may now be implemented in provenance-store.mjs with a focused suite; source and independent verification acceptance will be recorded separately.

The phase10r0 promotion guard passed2026-09-06T08:01:33Z with only five released specification paths changed: the new amendment, three current-set notices and a scope addendum to the still-unapproved D draft. All other source/test/spec/runtime paths were unchanged. The new amendment's SHA-256 is `3aa427afc9fd0979df644bc53cd0d3d3c97527c7fa50127599c363f0c636eb98`; its exact contract is authoritative. No code or test was changed by this promotion.

Root's27-case prospective audit is prepared but unexecuted. Five earlier fixture preflight rows used existing validators only; they established valid synthetic test data and one persisted error-order premise, not new helper behavior or an actual external reviewer invocation. No Linux execution or READY claim follows.
