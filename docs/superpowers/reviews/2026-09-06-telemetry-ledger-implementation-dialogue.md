# E1 telemetry/ledger implementation review dialogue

Status: **E1 IMPLEMENTATION ACCEPTED after fixed-candidate verification; see 2026-09-06-telemetry-ledger-codex-review.md.** Phase8f1/f2 followed the actual d6/d7/d8 discussions. Earlier present-tense statements below are historical checkpoints. The E1 specification is accepted as TP1.18; see 2026-09-06-telemetry-ledger-spec-codex-review.md. D remains open. Codex reviews; real Claude Code uses claude-opus-5 with xhigh under the existing project-only Anthropic authorization. This status update follows acceptance and does not replace the immutable candidate evidence.

## Initial independent observations

The first three controls reproduce actual defects, so their probe exits are **1**, not passing acceptance results. Each records an unchanged source hash during the observation. They are not simulated consumer-error properties and not claims of continuous race resistance.

| Item | Actual observation | Proposed correction for discussion |
|---|---|---|
| R1 read containment | A real Windows junction at `.ctide/output` points outside the temporary repository. A regular valid bound sidecar behind it is accepted. Leaf checks do not check the parents. | Apply existing bounded containment/type guards before both sidecar and initial artifact reads, as amendment C.2 requires. |
| R2 current-head recheck | After the real consumer passes, a source-copy wrapper changes the actual current head's kind but preserves its ref string and batch digest. Actual current validation refuses E_SHAPE; the collector still publishes numeric semantic counts. | Validate the later current store and complete typed head/task/base/inventory identity before extending the earlier consumer proof. Preserve initial stored facts on failure. Discuss the minimum checks without demanding equality of unrelated current-store bytes. |
| R3 unavailable collector | The actual CLI is copied alone so its real dynamic collector import fails. A valid requested task still yields one appended record and exit0, but `taskId:null`. | Preserve fail-open append/exit while using unknown for requested but unobserved authority. Absent/malformed requested identity remains null. |
| R4 source encoding | Literal NUL bytes in observer comments, delimiters and regex cause text search to classify JavaScript as binary. | Use textual escapes preserving runtime values; ordinary source hygiene. |
| R5 documentation | The reference says absent identity emits a diagnostic and calls the default all-unknown, contrary to the actual silent absence and null/false/unreported distinctions. | Correct those descriptions and document the standalone observer/sidecar and pending D ordering. |

Root reproduction scripts and receipts are under `.ctide/collaboration`: `e1-sidecar-read-guard-review.mjs` / `e1-sidecar-read-guard-draft.jsonl`, `e1-current-head-recheck-review.mjs` / `e1-current-head-recheck-draft.jsonl`, and `e1-ledger-unavailable-collector-review.mjs` / `e1-ledger-unavailable-collector-draft.jsonl`. R1's fixed-candidate assertion is additionally tightened to the existing exact E_OBS_TARGET code; the original failure receipt is preserved.

Separate draft diagnostics passed with all product-script hashes unchanged: eight real emitter → observer CLI → writer → consumer → collector → ledger CLI flows, plus twenty absence/API/CLI states (`e1-draft-diagnostics.json`). The flows distinguish oracle-only, SUT-only, declaration-only, moved-only, moved with hook, overlapping oracle/declaration, added and deleted changes. They verify exact block/sidecar fields, actual CLI exits, ledger-preserved freshness and postcommit observer refusal with prior bytes preserved. The absent-flag check is subsequently tightened to require no stderr diagnostic for fixed-candidate verification.

Forty-six further real ASSUM collector observations passed (`e1-draft-assum-diagnostics.json`): three prior-transition workflows and forty-three semantic/group/dedup controls. The last set includes two findings citing the same validated transition; the count remains one. Positive prior groups, absent groups with verified singleton acknowledgements, equivalent duplicate groups and negative evidence/resolution cases are exercised through the actual writer and consumer. Invalid consumer proof leaves all semantic counts unknown. These root-owned optional diagnostic hooks live outside product/test sources; they add no runtime request parameters. Earlier protected consumer probes run unchanged unless the explicit E1 flag is supplied.

These evolving-source diagnostics establish specific observations only. No candidate synchronization or final acceptance has occurred. After phase8i1 ends stable, Codex must inspect the final submission, discuss the remaining R1–R5 items with Claude, record both parties' reasons, release only agreed corrections and independently verify a fixed candidate.

## Stable submission and discussion dispatch

Phase8i1 ended actual exit0, 98 turns, 2093.3 seconds. The exact release guard passed at 2026-09-06T04:36:38Z: only the eleven released paths changed; all other captured source/test/spec/runtime paths stayed unchanged. Five final targeted suites passed with actual exits0: observer18/18, block14/14, head56/54/2skip, base-view30/30, ledger46/46 (164 total,162 pass,2 skip,0 fail). Unchanged ledger sibling suites also passed: reconcile42/42 and consolidate25/25. Claude reported direct structure validation passed; full regression has not run for this candidate.

During its own structure check, Claude independently corrected R4 using canonical JSON tuple keys. Codex inspected that both insertion and lookup consistently use the new rich/test-reference keys. This was an implementer-discovered routine correction, not a prior jointly discussed conclusion. The observer's submitted SHA is c8ac212da9e4b99fbecf351c174d64807e2c08314773235b3d030500fe32132a. The block and ledger source hashes remain identical to the R2/R3 defect reproductions. Codex inspected the submitted read paths and confirmed R1 still lacks parent checks.

Claude explicitly disclosed its shipped ASSUM tests cover only0/unknown and lack a nonzero positive. Root's46 independent cases already pass, but shipped regression also needs that meaningful positive/dedup coverage. This becomes R6 alongside labelled returned-verdict/current-head controls. The read-only phase8d6 prompt requests reasons on all items and specifically asks whether an unrelated valid append should invalidate an otherwise unchanged named head. It does not demand blind current-store-text equality after Step5. Its freeze covers all162 current source/test/spec/runtime paths, permitting no changes. No R1/R2/R3/R5/R6 correction is released while this discussion is pending.

## Actual reasons and settlement before corrections

Phase8d6 completed actual exit0,3 turns,147.445 seconds; its162-path read-only guard passed with no changes. Claude explicitly agreed with all three defect reproductions after checking source. It explained that R1's leaf lstat cannot protect a parent junction and a later publication guard cannot undo an earlier read. For R2 it accepted current-version/schema/full validation, full typed head, resolved record task/kind/digest, full task base witness and derived inventory identity. It argued that an unrelated valid append must not downgrade counts because C.3 binds the same named head and digests, not the entire changing store text; Codex accepts that minimum. Invalid current authority still downgrades semantic counts while initial stored facts remain. R3's conditional fallback uses unknown for a requested identity when collection fails, preserving null for no usable request and the existing fail-open append. R4's internal tuple encoding and R5's literal/diagnostic/discoverability corrections were accepted with their actual chronology preserved.

Claude raised the additional current-store validation and fresh-clock cost before edits. Codex accepts that existing current-authority recheck: validateAll at the later capture's instant may reject an expiry-sensitive applicability invariant. Its clock reach is specifically validateInvariants/applicable and validateReopenCauseCoherence/applicable, not every grant/Source check or continuous freshness. The accepted consumer's own single-T0 protocol is unchanged. Claude also corrected its attribution: the sentence against blind whole-text equality was Codex's prompt, not a quotation from the amendment; C.3 supplies the normative basis.

Codex found two errors in Claude's proposed R6 tests and requested another read-only discussion before release. First, mixing a wrong-subject or general finding into an expected-converging positive world is impossible: the actual consumer rejects general findings at1063-1073 and wrong ASSUM pre-binding/transition subjects at961-986. Those must be separate unknown-count negatives, not silently skipped positive counts. Second, appending before collection cannot detect an incorrect whole-text-equality recheck; both reads would already see the same new text. The valid append must occur between the collector's own reads.

Phase8d7 ended actual exit0,3 turns,68.531 seconds; all162 protected paths remained unchanged. Claude acknowledged both errors with the relevant source predicates. It revised the positive to two valid ASSUM findings citing one transition (finding count2,transition count1), with separate actual-consumer-refusal negatives. It agreed to a labelled source-copy store-import wrapper that uses the actual append-source writer immediately before the second collector-owned load, after the real consumer pass, proving changed text but valid unchanged head/base/batch/inventory identity and preserved numeric counts. R1's parent-junction test will count actual artifact reads and require zero before E_OBS_TARGET, rather than accepting a later publication error. Codex accepts these reasons and corrected tests.

Only after this settlement, phase8f1 was released for exactly eight files: observer/block/ledger scripts, their three tests, ledger reference and runtime contract. Its guard captures162 paths. No producer/discovery/writer/consumer/head/base/spec change or D code is authorized. The three affected suites run through the actual-exit runner; Codex will then inspect and verify a fixed candidate. Root's independent E1 suite now prepares41 rows, adding the no-artifact-read parent guard and the between-read valid-append discriminator to its earlier39. These expanded checks have not yet run against a fixed corrected candidate.

## Correction result and evidence precision

Phase8f1 ended actual exit0,34 turns,892.254 seconds. Its exact eight-file guard passed at2026-09-06T04:59:20Z. Final affected suites passed88/88,0 skipped,0 failed: observer20,block21,ledger47. Codex independently ran five corrected-boundary controls with actual exits0: sidecar parent refusal, artifact parent refusal before any read, invalid typed-head downgrade, a real file-backed valid append between the collector's own reads with counts retained, and unavailable-collector identity fallback. These are evolving-source diagnostics, not fixed-candidate acceptance. Fixture failures during implementation are retained in the logs rather than presented as passing runs.

Codex found two final precision issues and requested read-only phase8d8 before their corrections. First, the ledger reference called the complete after-emission/before-commit order enforced, while the implemented text-digest check enforces only the precommit boundary. Claude agreed that the observer never sees the emitter's return and cannot prove preceding invocation; D must establish that sequence. The runtime contract already states the narrower boundary correctly and needs no symmetry edit.

Second, the timing-positive shipped test used applyTransaction followed by manual fixture persistence. Claude confirmed that this is the production domain transformer, but bypasses the actual file-backed writer's load/lock/CAS/atomic-publish path. It explained why runTransaction can serve this test without deadlock: no store lock is held by the collector's plain reads, append-source needs no batch CAS expectation, and the wrapper already has cwd. The agreed change calls runTransaction immediately before the second collector-owned load and removes manual persistence in that branch. Deliberate corruption negatives remain manual tampering.

Codex accepts the other synthetic-envelope counter/resolution fixtures as component evidence through the actual consumer. Claude agreed to label their constructed inventory/body/testRef and domain-transaction-plus-fixture-persistence scope honestly; they do not prove actual producer derivation or the full file-backed commit workflow. The independent eight E1 workflows and46 ASSUM collector controls provide that stronger evidence. Rebuilding those shared fixtures is unnecessary for their purpose.

Phase8d8 reported successful completion,3 turns,54.031 seconds. Its162-path read-only guard passed at2026-09-06T05:03:39Z with no changes. The previous turn had already closed its PTY; this resumed turn confirmed the successful result and did not invent another exit observation. Only after these reasons were reviewed did Codex release phase8f2 for the ledger reference and block test, with that21-test suite alone required before END stable. This is evidence/documentation correction, not an additional product defect or D acceptance.

Phase8f2 ended actual exit0,7 turns,75.737 seconds. The affected block suite passed21/21,0 failed,0 skipped in19.6 seconds. Its two-file guard passed at2026-09-06T05:05:31Z, with all other protected paths unchanged. Codex read the final wording, domain-fixture scope comment and the actual runTransaction timing branch, which returns before manual corruption parsing/persistence. Both changes match the settled reasons. The ledger reference SHA is 0291030bcc02096aee3eee97d328bfd143e6d74f8fda50bc97f05411f9dd29df; block test SHA is 05c9f60e77de12efc9a2e01a398f9c4ecfec14ecd06ba86ce53be8c2824af022. Fixed-candidate full and independent regression now follow. Final acceptance, if earned, will be recorded separately in the implementation review.
