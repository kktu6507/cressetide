# Live plan-review channel: independent acceptance

Accepted for one bounded, plugin-free capability observation on Windows, 2026-09-09. Real Claude Code completed plan → separate read-only opinion → separately authorized revision → exact-plan approval → implementation. Root independently verified raw output, actual spawn/argv/stdin, source identities, guard evidence, final tree and the two existing public tests. This is progress beyond the [offline phase27 acceptance](2026-09-09-plan-review-feasibility-codex-review.md), but is not native Vigil integration, full-flow measurement, an efficiency optimization, or proof of real-world benefit.

## Candidate and prospective scope

Phase28a discussed the misleading unconditional simulated label before the separate phase28b patch GO. The only code changes added operator-declared evidence origin, canonical labels and binding checks. An origin declaration does not prove a provider call; independent actual CLI dispatch evidence supplies the operational provenance. Prompts, fixtures, caps, tools, guards and transitions stayed unchanged. The accepted phase27 bytes remain archived under `.ctide/collaboration/phase27-accepted/`.

Root verified the origin patch: **57 tests,56 passed,one Windows symlink privilege skip,zero failures**; all26 earlier independent cases and three separate origin scenarios passed. All123 protected files matched. The declared-live offline test still used a fake spawn and is not counted as live evidence.

Before dispatch, phase28d GO and `phase28-prospective-live.json` fixed one new root, source/CLI/orchestrator hashes, four calls in order, one discussion cycle, US$4.25 policy ceiling and no retries/replacement root. Only the actual plan was reviewed; canned synthetic findings were not reused. The fixture and capability claim were fixed beforehand, with no hidden grader or product-effectiveness claim.

## Observed sequence

All four calls used session `6310a440-b960-4efa-ba2d-52c0bd0ac5dd`, actual model `claude-opus-5`, CLI2.1.263. Effort was requested as xhigh; it was not independently observed. Initialization listed only Read/Grep/Glob for all three text-planning calls, with observed permission mode `default` from requested `manual`. Implementation exposed the six approved tools with `acceptEdits`. No plugins, MCP servers, other model usage or spawned children were observed.

| Call | Actual result | CLI cost estimate USD |
|---|---|---:|
| Plan | Correct source repair proposed; root had not approved it | 0.0878325 |
| Opinion | Agreed on exact verification commands; explained syntax-versus-behavior evidence and acknowledged contradictory prose | 0.115161 |
| Revision | Superseding plan incorporated the separately recorded resolution; stopped for approval | 0.0955895 |
| Implementation | One approved source edit; both frozen commands ran separately and succeeded | 0.219455 |

Root clarified that the initial prompt had not supplied the exact command allowlist. The plan's generic `node --test` proposal therefore was not described as knowingly ignoring an instruction. The clarification asked for reasons first, then root recorded the resolution and requested a revision. Only after independently reviewing that revision did root approve SHA256 `ac0e19f14926412e9ea63ff4715327032a70e7d7351a44b0d16573d735147101`.

The actual implementation stdin contained that exact revised plan and root resolution. The fresh/read-only/resume boundaries were observed directly, rather than inferred from the model saying it followed them. Actual implementation tools were one Edit on `src/widget.mjs`, then `node --check src/widget.mjs` and `node --test test/widget.test.mjs`; no composed or substitute command ran. Guard evidence and source scope passed the fixed protocol.

Precision from Claude's read-only phase28f cross-review: the resumed opinion and revision calls issued **zero tool calls**. Their read-only surface is evidenced by CLI initialization declarations, absent editing tools and unchanged files, not by executing read tools or attempting forbidden edits during those calls. Implementation exercised the allowed guard path; no guard refusal occurred in this observation, so its live denial path remains unexercised here. Root accepts this qualification.

## Independent result and limits

Root's [live audit evidence](2026-09-09-plan-review-live-capability-evidence.json) records zero faults, mechanically accepted state `IMPLEMENTED`, all123 protected files unchanged, and separate root execution of both public commands. Syntax parsing exited0; the two public tests passed2/2, with0 failures. README and tests were unchanged. These tests do not establish array identity/non-mutation/non-boolean edge coverage, and are not hidden independent confirmation.

The four reported cost estimates sum to **US$0.518038** under the protocol's per-process convention, below the US$4.25 ceiling. Raw usage/cache/model/child counters are retained. This does not independently prove the convention's billing interpretation, and the amount is not subscription billing or total collaboration spend. It excludes all implementation/discussion sessions and is kept separate from the closed pilot's US$4.4346125; neither observation replaces or rescues the other's results.

The capability is verified for this one fixture/configuration: a resumed text-planning session kept its read-only surface, and the real review/resolution/revision/approval transport reached guarded implementation. No claim follows about native Vigil panels, aggregate full-flow costs, comparative efficiency, GPT-6 Astra runtime benefit or general reliability. Historical process/snapshot records remain internally reconciled observations, not externally authenticated history; TOCTOU and post-hoc read-scope limits remain. The offline symlink branch is still unverified under current Windows privileges.

## Roadmap consequence

The missing review-channel capability is now both implemented and observed live. L0 remains partial; the original L1a pilot remains closed with its original measurement limits; L2 has no evidence-backed avoidable cause/candidate; L3 lacks independent confirmation and eligible closed-task data; L4 retains the current default provisionally. None is relabeled complete by this capability result.

The next useful product decision needs cost/quality evidence from an actual workflow. Synthetic full-flow work is technically possible, but it cannot substitute for the real-world benchmark contract's closed-task history. Root has requested an existing eligible record path or a genuine upcoming development task as a prospective collection starting point. No extra experiment, instrumentation expansion or prompt optimization is silently authorized by this acceptance.

Claude's final read-only cross-review found no material factual disagreement or overclaim. Both reviewers found no evidence-backed product edit justified by these observations. The highest-value next input is an eligible real-task record or a genuine upcoming task; existing collection tools should be assessed against that concrete task before adding an adapter. The planner's missing command allowlist is retained as an instrumentation design observation, not a demonstrated Cressetide cost cause or an authorization to reopen the finished observation.
