# Efficiency exploratory checkpoint — incomplete roadmap

The two prespecified positive task observations have ended. Neither reached implementation. The current product default remains unchanged, and the efficiency roadmap is **not complete**.

## Verified outcomes

| Observation | Instrument result | Independent disposition |
|---|---|---|
| S2: single-module flag repair | Failed the frozen recovery prerequisite | The allowed command did execute the expected red baseline, but its nonzero exit could not satisfy the required nonerror-result recovery condition. Original failure retained; no implementation approved. |
| S3: two-module forwarding/partition repair | Passed with recorded limitations | The plan proposed folding code review despite declaring lint/typecheck/build NA; current 1C requires those checks green. Root rejected the plan as submitted. No implementation approved. |

The [independent per-plan evidence](2026-09-09-efficiency-positive-v2-plan-evidence.json) binds raw digests, plan hashes, model usage, actual test outputs, guard audits and unchanged source bytes. Both invoked native `ctide:vigil` with Opus5 observed and xhigh requested; runtime effort was not independently observed. They used no child agents and preserved the consumer fixtures. S2 reproduced3 tests/1 pass/2 expected failures; S3 reproduced4 tests/2 passes/2 expected failures. An instrument's `ok` does not establish semantic plan approval.

S3 also pre-committed against future automatic test-review substitution without establishing a written exclusion. Eligibility requires actual post-change evidence, which was never reached; this is a plan-level inconsistency, not a count of an executed substitution failure. The implementation proposal itself appears consistent with the public acceptance criteria by inspection, but has no observed corrected behavior. Both plans correctly disclosed unavailable tools, NA checks and the approval boundary; S2 correctly declined the ineligible code-review fold.

The confined protocol cannot record a plan revision and deliver the corrected plan through its fixed resume prompt. The raw S3 plan will not be edited or implicitly approved with an unseen amendment. No additional case or relaxed tolerance was added after seeing outcomes. S2 and S3 are different tasks and denied commands, not a controlled pair differing only in event order; their costs cannot isolate a causal effect or a product cost saving.

## Tooling and remaining verification

Root verification found defects in recovery ordering, result matching, exact error recognition and plan-path qualification in the private measurement tool. Claude Code first responded in read-only discussions, disagreements were resolved, and a separate implementation authorization followed. Independent verification then passed50 of51 tests, with one Windows link-privilege skip and zero failures; root counterexamples produced their expected outcomes. Shared helpers, guards, frozen fixtures, archived v1 bytes and all110 distributed plugin files remained unchanged. The persistent instrumentation's earlier full Windows receipt remains2,132 tests/2,120 passes/12 skips/zero failures; these later private-file changes are covered by the scoped51-test run, not retroactively by that receipt.

After both blind observations were closed, the original hidden payload was materialized with its pre-call SHA256 unchanged (`31833b7506170143bfeb8555062eafd9e2dbd3200ae148e6c9703e8d90ebc25f`). Running the grader on the **untouched faulty fixtures** detected18 failing assertions among30 S2 checks and23 among90 S3 checks. These are grader negative controls only. They do not grade an implementation, establish corrected behavior, or constitute separate L3 held-out task confirmation. The grading inputs are now exposed to the maintainer workspace and must not be reused as fresh blind validation for a future candidate.

## Roadmap disposition

The hidden-payload SHA above identifies the realized grading bytes. The separate freeze-metadata file has SHA256 `3910a2bd5870dfae144960c9207a25cff2b3391693f8838ad123f79f768d94a8`, which is the harness's metadata binding. Different artifacts have different digests.

L1a closes by its original explicit-measurement-limit exit condition. That qualified exit does not establish the unavailable full-flow quantities or complete the remaining roadmap stages.

| Stage | Status | Remaining dependency |
|---|---|---|
| L0 | Partial | Full-workflow accounting and outcome validation were not obtained. |
| L1b | Accepted within its prior bounded scope | Seven reviewer/judge pairs and four judge-calibration controls do not establish whole-workflow or real-world efficacy. |
| L1a | Prespecified attempts closed with measurement/protocol limits | Full-workflow cost, corrected behavior and executed panel/control evidence remain unavailable. |
| L2 | No cause or candidate selected; implementation unexecuted | An observed, avoidable product cost cause and relevant validation are required. Model plan-adherence issues are recorded but not established cost causes. |
| L3 | Unexecuted | A candidate, a prospectively frozen independent confirmation set and, for real-world claims, eligible closed-task ledger history. No eligible `.ctide/ledger` exists locally; the request for an existing data path is pending. |
| L4 | Current default retained provisionally; evidence gate unmet | This is not validation of the current default or completion of the evidence-backed final decision. |

Progress beyond this checkpoint requires a new prospective measurement design that can support review/discussion/plan revision, plus the missing outcome evidence. It must preserve these attempts and count their costs, define its rules before new outcomes, and use new blind validation data if a candidate is later proposed. This document does not authorize a retry or pretend those dependencies are completed. No product prompt reduction, reviewer merger, new mode or model downgrade was made.

The cumulative experiment estimate is **US$4.4346125**, including failed experiments and the separate resume diagnostic, but excluding **all implementation and discussion sessions**. The two v2 plan phases contribute US$1.615614. These are CLI price-table estimates, not provider billing or the total cost of this collaboration. No whole-workflow cost, directional bound, saving, Linux result or GPT-6 Astra runtime result is established.

The detailed root/Claude dispositions are retained in `.ctide/collaboration/phase26k-disposition.md`; the predeclared control table and its scoped observations are `phase24f-controls.json` and `phase26j-control-observations.json`. Findings were discussed before any proposed correction; no product correction was authorized from these observations.
