# Lower public request boundaries: reasons before correction

Scope: direct JavaScript exports used by the accepted provenance components. This does not approve D's controller or change domain authority. Codex owns planning, independent probes and acceptance; real local Claude Code uses claude-opus-5 with xhigh effort in the existing authorized project-only session.

## Findings and phase10q2 discussion

The earlier q1 acceptance explicitly retained lower exports as unresolved. Codex then reproduced two material failures in isolated real Git repositories, using ordinary mutable requests rather than getters: withStableHeadView compared S1 from repository A against S2 from B, accepted equality, and returned despite A changing during a non-writing evaluator; buildDiscoveryAnalysisPreimage paired A's captured base with B's distinct head. Separate18-row boundary controls completed actualexit1,16fail/2pass: incomplete own-key checks, repeated values and replacement of the validated callback. Base capture already read its values once; the callback's original receiver was a passing compatibility control.

Phase10q2 was READ-ONLY. Actualexit0,18turns,335.795seconds;165-path guard at07:16:00Z found no changes. Claude read the probes and source, agreed with reasons, and distinguished two independent stable-view defects: the repository identity could move, and the invoked callback could differ from the validated callback. It also identified the same mismatch for config-carrier state, and discovery's later return of request.baseTreeOid could declare a different tree from the one actually read.

Claude verified that captureHeadViewSnapshot's repeated raw root parameter only supplied diagnostics, so replacing it with the captured root preserves valid behavior. Discovery's fresh registry read keeps its existing external-observation order. Base's current value capture remains unchanged; its own-key guard alone needs correction. Both parties agreed to safe Symbol refusal, complete own keys (including permitted nonenumerable properties), and one capture of each owned value. No prototype prohibition, hostile-Proxy guarantee or unrelated JSON schema sweep is added.

The callback receiver is preserved deliberately through Reflect.apply(capturedEvaluate, originalRequest, [snapshot]). Claude explained that a bare captured call changes this to undefined in module strict mode, breaking an already-valid observable behavior. The correction fixes which function the module invokes while leaving its established receiver and exception identity intact.

## Released correction and independent evidence

Only after reviewing those reasons did Codex release phase10q3 for exactly three modules and three existing suites: head-view-snapshot, adapter-discovery-preimage and adapter-content-view. No D, spec or other source file is editable. The release requires meaningful source/callback/base-identity regressions, preserved carrier checks, own-key controls, a faithful limited negative control and restored final targeted results. Codex declined four reversals times all entire suites: accurate reporting of what actually ran resolves the earlier evidence wording mistake without twelve redundant suite executions.

Codex independently reproduced both additional semantic cases against the accepted q1 snapshot: a tracked-to-untracked config flip kept the digest identical but was hidden by redirecting the request to B; a request's baseTreeOid changed to another actual tree whose base declarations differed, and discovery returned that new identity alongside the original tree's declarations. Both rows failed with actualexit1. The independent lower audit now comprises22 rows across three plans. None has yet run against the completed correction, which is still being implemented.

The remaining request-shaped public exports were inventoried separately: analyzeModule/analyzeView, readExactTreeBlob and verifySourceFreshness. A17-row actual baseline has1pass/16fail, including analysis following a replacement module list. Those three modules are REASONS ONLY in phase10q3, not part of its six-file editing release. They cannot be counted as corrected through callers' captured literals.

## Evidence scope and D separation

Claude accepted Codex's q1 evidence corrections: its collector accessor test used an absent-store path; root's separate full-flow audit covered the committed collector. The q1 reversal executed only the emitter suite, so saying the four other suites also passed under reversal was incorrect. Their final restored results remain valid. No source defect was inferred from the reporting correction.

D11's cleanup/global-collision/diagnostic principles were reviewed separately. D12 still asks for exact prospective validation order and staging, typed ruling freshness without imposing review-ruling packet fields on plan gates, and finite cleanup/diagnostic rules. D is unapproved; implementation of the lower boundary correction establishes no controller acceptance.

## q3 stable submission, independent behavior check and q4 release

q3 ended actualexit0,46turns,775.542seconds. Its165-path guard passed07:31:06Z with exactly the six released files changed. Claude's final three suites total127tests/125pass/2skip. Its limited use-site reversal ran five selected head tests (1pass/4fail) and three discovery tests (0pass/3fail), actualexit1 each; the surviving capture reads mean those reversal counts do not reproduce the original getter counts. That limit was disclosed. The final three suites ran after restoration, not under reversal.

Codex reviewed all six diffs and found a shipped-fixture defect: the first two new head tests perform writes inside evaluate despite the explicit agreed non-writing-evaluator scenario. The carrier case also starts B without config, so it discriminates a wrong diagnostic subject rather than the intended false stability acceptance. This is an implementation lapse against the already-discussed test contract; the independent source behavior may still be correct.

Codex fixed77 candidate files and executed all22 independent lower rows with actualexit0 and matching manifests. Both equal-head and equal-digest/carrier adversaries put the mutation outside the evaluator, and the declared-base case uses two actual trees with different declarations. These pass against the q3 source. Receipt: lower-first-candidate-lower-request-probes.json. This scoped behavior check is not a full regression acceptance, and it does not validate the flawed shipped fixtures.

q3 Part C agreed with the last three-module correction, distinguishing synchronous accessor divergence from the earlier ordinary mutation across await. Codex accepted that severity distinction, preserved analyzeModule's existing single reads, analyzeView's branded view and pre-await list copy, tree-reader error order, and S3 parser/capture/registry order. q4 then explicitly released those three modules and three suites, plus repair of the two named head tests to the already-agreed premise. The head source is editable only for a temporary negative control and must finish at the exact q3 SHA88792cac35fb75401b3f29d1cc33eae119bb80d3e246f6daf920ee1b7aff9893. Any disagreement must be explained before the dependent correction.

The same q4 round may revise only the one UNAPPROVED D draft, incorporating settled d9-D12 principles and explicit staging/coverage/projection clarifications. No controller, prospective helper, runtime/prefix, caller, or normative version promotion is released. Root will review the new draft before any dependent implementation.

## q4 final submission and source review

q4 ended actualexit0,62turns,1035.134seconds. The165-path guard passed07:52:55Z: exactly the three remaining source modules, four named suites and the one unapproved D draft changed; final head source restored the exact q3 SHA. Codex reviewed all final six lower sources and six suites and recorded their hashes in lower-root-reviewed-files.json before independent combined verification.

Claude explicitly agreed that its two q3 head tests contradicted the module, its own proposal and the release. It explained that the first test's illegal evaluator write contaminated the demonstration, while the second already refused under reversal and only exposed the wrong diagnostic identity. The corrected evaluator only signals entry, awaits an external gate and returns; the caller mutates outside it. Both carrier fixtures start tracked with equal digests, and A's later index-only change leaves its digest unchanged. Early capture failure is raced against the entry signal. Older AC134 remains untouched.

The scoped head reversal selected exactly those two tests and both falsely accepted, producing actualexit1. The analyzeView list-use reversal selected the actual one.test.mjs versus two.test.mjs discriminator, which failed actualexit1; the log records two executions within that one reversal. No exact original getter-count claim follows because other captures remained. After restoration the four affected suites total175tests/173pass/2skip: head62/60/2, node87, tree4, S3 22, every actualexit0. The exact-tree suite is new; before-test source verification executed the existing node84 and S3 20 suites, not an imaginary pre-existing tree suite.

The completed code is pending full independent fixed-candidate acceptance. The s4 D draft remains a separate unapproved submission; its status does not affect whether these twelve source/test files correct the agreed direct API defects. No D production source was released.

## Independent acceptance

The preceding paragraph records the submission checkpoint. Codex accepted the correction at2026-09-06T07:59:37.265Z after all fixed78-file checks passed and matched original project bytes. Full1756tests/1744pass/12skip/0fail, direct structure and deterministic eval7 passed actualexit0. Independent lower22, remaining17, public28, preview25, writer109, consumer144 including46 E1 observations, integration96 and E1 41 all passed with matching manifests. See the separate lower-request implementation review and immutable acceptance receipt. This decision accepts the bounded source correction; it does not approve the s4 controller draft.
