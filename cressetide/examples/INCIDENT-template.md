# Incident: <short title>

- Status: open
- Started (UTC):
- Severity:
- Impact:
- Incident commander:
- Current health signals:
- Known unknowns:

## Timeline

## Evidence

## Decision cards

## Re-entry criteria

## Closure

Flip the `Status:` line at the top to `closed` only after every required closure
check has actually completed; the `open | mitigated | closed` field and its
checklist are owned by
[`skills/salvage/references/reentry-and-closure.md`](../skills/salvage/references/reentry-and-closure.md).
`/ctide:doctor --project` reads that exact line and treats a missing or
unparseable one as unknown, never as closed.
