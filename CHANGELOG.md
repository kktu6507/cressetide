# Changelog

All notable changes to Cressetide will be documented in this file.

## [Unreleased]

## [0.6.0] - 2026-07-24

- `destructive-guard.js` 新增 6 條狹義 infra／data-store destructive-command patterns，涵蓋 IaC 整體拆除（`terraform`／`pulumi`／`cdk destroy`）、kubectl namespace 刪除（`kubectl delete namespace`／`ns`）、容器 volume 資料流失（`docker volume rm`／`prune`）、Postgres 刪庫（`dropdb`）、MySQL 刪庫（`mysqladmin drop`）、Redis 全庫清空（`redis-cli flushall`／`flushdb`）：沿用既有的 quote-stripped `unquoted` 字串與行首／分隔符錨定慣例，無 raw-vs-mask split，新舊 pattern 全部吃同一份 `unquoted`。`permissionDecisionReason` 同步擴充列舉，並新增一句框架說明：部分新涵蓋指令（kubectl、IaC、DB-admin CLI）可能觸及遠端或共享／production 系統，不只是本機。同步更新 `docs/runtime-contract.md`、三語 README（`README.md`／`README.zh-TW.md`／`README.ja.md`）與 `test/plan-gate-guards.test.mjs`／`test/hooks.test.mjs` 兩份測試檔。
- `destructive-guard.js`／`plan-gate.js`／`load-failure-memory.js` 修補前一則 mysqladmin／redis-cli ReDoS 修復當時揭露但未修的 9 處無界 `[^;&|]*` 站點，加上 navigator 全庫掃描額外找到的第 10 處：`destructive-guard.js` 的 `git reset --hard`、`git push --force`、`find -delete`、`dd of=`、PowerShell `Remove-Item`／`ri`；`plan-gate.js` 的 `sed -i`、`perl -i`、`dd of=`（與 `destructive-guard.js` 同款逐字元同步，`.github/scripts/validate-structure-core.mjs` garden 9d 有機器閘釘住這條）、`git apply`（否定型 lookahead，病態輸入方向與其餘正向 lookahead 相反）；以及威脅模型不同的 `load-failure-memory.js` 的 `isRetired`（`[^)]*`→`[^)]{0,200}`，界的是 `(...)` 括號內容長度而非 shell 分隔符間隔，處理的是可能遭惡意 repo 精心構造夾帶的 FAILURE_MEMORY.md 內容）。全部沿用既有 `{0,200}` 界限慣例；每站皆以 old-vs-new regex 對照量測輸入倍增下的耗時比值（約 4.0x→2.0x，即 O(n²)→O(n)），並對每站既有宣告的 ASK/DENY/ALLOW/retired 判定逐一確認零差異。新增 12 個站點的 F1-style 效能回歸測試（10 個新界限站點＋2 個先前 mysqladmin／redis-cli 修復時遺漏的回歸計時測試，regex 當時已界限、僅補測試；共 21 個 test()，比照既有 `rm -rf` F1 樣板的「真實長度校正性」＋「病態輸入計時」配對，分見 `test/plan-gate-guards.test.mjs`、`test/session-memory-hooks.test.mjs`）；測試總數 723 → 744，`npm run validate` 通過（含 garden 9d 的 dd-of= 逐字元同步檢查）。
- 新增第五個 skill `/ctide:ship`（`cressetide/skills/ship/`）：手動觸發、純唯讀的 release-readiness 決策卡產生器——讀取 run ledger（上次 `vX.Y.Z` release tag 之後、依該 tag commit 的 committer date 篩選的 `READY` run）、repo 內每份 `package.json`、`CHANGELOG.md` 的 git diff 歷史、git tags，以及 `SYSTEM_MAP.md` Rollback 章節的 `CTIDE:TRUST` sentinel，輸出五項檢查（版號一致性、changelog 是否改過、tag 是否就緒、migration compatibility、條件式 checksum 驗證）——每項皆為 `pass`／`fail`／`not-applicable`／`unverified` 四態並附引用證據，從不執行 build/test/deploy、從不挑選或更動版號、從不寫入任何東西（工作樹、`.ctide/`、任一會改變狀態的 git 指令，一律不碰）。`scripts/ship.mjs` 的 flag parser 沿用 `run-consolidate.mjs` 的 `KNOWN_FLAGS`／`get()` swallow-guard 樣式、git shelling 沿用 `map.mjs` 的 `git()` helper（`spawnSync`＋顯式 piped stdio）、migration-compat reader 沿用 `map.mjs` 的 `OPS_TRUST_TAG_ATTEMPT`／`opsTrustTagIsWellFormed` 機制（只讀機器 sentinel、絕不解析周邊 Rollback 散文）——刻意不沿用 `map.mjs` 的 `args()` 與 `doctor.mjs` 的 `argument()`，兩者皆有未修的 next-token-swallow flag-parsing 舊病。checksum 驗證僅在明確帶入 `--artifact`／`--checksum` 兩個 flag 時才跑，絕不啟發式掃描目錄猜測建置產物；版號一致性僅讀 `package.json`，不涉及其他 manifest 生態系——此兩項為規劃階段與使用者一起定案的範圍收斂，非實作疏漏。新增 `test/ship.test.mjs`（57 個 test，含以整棵工作樹 byte-comparison 證明、且已驗證真的會轉紅的零寫入不變量，以及 fail-open／exit 0 測試）與 `eval/cases/ship-rollback-relevance-routing.json`（讀 Rollback 散文本身判斷與待處理批次的相關性、並將版號／changelog 落差導向 `/ctide:vigil --lite`、checksum 不符導向重新 build 而非 code fix 的路由邏輯，屬 coordinating thread 的語意判斷、非 `ship.mjs` 的機械化職責）。連帶更新 `doctor.mjs`／`doctor/SKILL.md` 的四→五 skill 計數、`docs/command-reference.md`（新增 `/ctide:ship` 章節）、`.github/scripts/validate-cressetide-extensions.mjs` 三處硬編陣列、`test/structure.test.mjs`、`eval/run-eval.mjs` 的 capabilities 集合、`docs/benchmark-contract.md`／`docs/evaluation.md`、三語 README（各新增一節、維持 17 節 parity）與 `ARCHITECTURE.md`（新增 `### Ship` 小節，規劃清單未列但同屬「four public commands」計數需同步更新的既有站點）。`cressetide/.claude-plugin/plugin.json` 無需改動——skill 由磁碟上 `skills/<name>/SKILL.md` 是否存在探知，並非 manifest 陣列註冊。測試總數 744 → 801，`npm test`／`npm run validate`／`npm run eval` 皆通過（eval case 數 6 → 7，`ship=1` 併入分布）。
- `.github/scripts/validate-structure-core.mjs` 兩條原本只掃 `cressetide/skills/vigil/` 的結構性 CI 閘，本次擴大為涵蓋全部五個 skill（doctor／map／salvage／ship／vigil）：garden 9a（孤兒引用偵測）的可達性判定改為「檔名只要出現在任一 `skills/*/SKILL.md` 或 `agents/*.agent.md` 中即視為可達」的全域聯集，不再侷限於引用所在 skill 自己的 SKILL.md；garden 9e（裸調用偵測）的比對範圍同步由 `skills/vigil/scripts/` 擴為 `skills/(?:doctor|map|salvage|ship|vigil)/scripts/`。相反方向的 5b（引用是否存在）同步從單一硬編例外清單（僅涵蓋 vigil→map 的 `vigil-map-overlay.md`）改為「先查自身 skill 的 `references/`，找不到再搜尋其他 skill」的 search-based 解析——規劃時直接驗證過，naïve 的「只認自身目錄」模型會誤判 salvage→map 的 `operational-readiness.md` 與 salvage→vigil 的 `external-capabilities.md` 兩條真實跨 skill 引用。`test/validate-structure.test.mjs` 新增涵蓋 doctor 的孤兒引用／裸調用注入案例，以及三條跨 skill 引用不得誤判的正面斷言。
- 新增 garden 9d 第 4 個群集：byte-comparison 守護 `ship.mjs` 刻意鏡像自 `map.mjs` 的 5 個構件——`git()`、`OPS_TRUST_TAG_ATTEMPT`／`OPS_TRUST_TAG_WELLFORMED` 兩個 regex 常數、`opsTrustTagIsWellFormed()`、`isValidIsoDate()`——任一方 drift 或無法擷取即 fail；兩份原始檔比照既有 hooks-family 慣例，逐一補上 `kept in sync … (documented copy — see garden hash guard)` 註記。`test/validate-structure.test.mjs` 新增 6 列參數化測試（5 個構件各自的單點 drift 案例，加上改名 `git()` 函式簽章觸發「無法擷取」而非誤判為 drift 的第 6 種案例）。
- 新增 `.ctide/decisions/` decision-record 慣例（`cressetide/skills/vigil/references/decision-record.md`）：比照 `incidents/` 命名為 one-file-per-decision 的 `DECISION-<YYYYMMDD>-<slug>.md`，而非 `FAILURE_MEMORY.md`／`EXPERIENCE.md` 式的單一成長檔案；`Status` 僅 `active`／`superseded (by <ref>)` 兩態、無自動過期機制，staleness 交由 `Revisit trigger` 欄位在下次相關規劃自然浮現，而非另立時鐘判斷。寫入面比照 `FAILURE_MEMORY.md` 既有「`arbiter` 決定、main thread 寫入」的單一寫入者分工，且僅由四項窮舉訊號觸發（reviewer 建議被明確拒絕、殘餘或 Extended-Safe 風險被接受而非擋下、規劃在兩個以上真正可行方案間做出選擇、feature／skill candidate 被整體否決），不接受開放式主觀判斷；`arbiter.agent.md` 新增對應的 Decision memory rules，`vigil/SKILL.md` 新增規劃階段唯讀諮詢與 Step 9 寫入時機。同一 commit 內含修復輪次修正：`references/final-report.md` 新增條件式 Decision Memory 表格欄位（compact 與 `--report full` 皆有）、`references/run-ledger.md` 的三態狀態說明補上 `decisions/`、`ARCHITECTURE.md`／`docs/runtime-contract.md`／三語 README 的 `.ctide/` 樹狀說明同步補上 `decisions/` 一行。
- `failure-consolidate.mjs` 新增 `tagRecurrenceCandidates()`：對所有未 retired 的存活 entry 兩兩配對，任兩筆 entry 的 `Tags` 欄位（以 `/` 分隔、trim 後轉小寫比對，避免大小寫飄移掩蓋真實重疊）交集達 2 個以上 tag 即視為候選，回報配對標題與共用 tag 作為 `arbiter` 的輔助證據——結構性、無時間窗，與既有 `consolidationReport()` 的 ledger-based staleness 判定（retired／expire candidate）彼此獨立。`references/final-report.md` 新增 `Tag recurrence` 欄位：compact 樣板無候選時整行省略；`--report full` 則是既有 Failure Memory 表格新增一列，無候選時顯示 `none`，該列本身不省略；`arbiter.agent.md` 新增對應段落，明確定調「機械式 tag 重疊是證據、不是自動結論，仍須判斷是否真的共享可重用的教訓形狀」；`docs/advanced/retro-practice.md` 新增一段，說明這是獨立於 `run-consolidate.mjs` 既有 escaped-closure tripwire 之外的第二套 recurrence 訊號。`test/failure-consolidate.test.mjs` 新增對應測試。
- `reviewer-common.md`／`review-packet.md` 的 Non-mutating 規則各補上一句：需要以「先 mutate 再觀察」方式驗證的情況（例如確認某個 regression test 在特定變更下真的轉紅），必須對 scratch／temp copy 操作，絕不可對共用工作樹本體操作——即使事後 byte-exact 復原也不足夠，因為平行執行的另一個 reviewer，或 orchestrator 自己的並行驗證，可能在復原前就先觀察到中間態。動機為一次真實事故：Tier-1 平行 review panel 中，一個 `code-reviewer` 為自我驗證修法而三次原地 mutate 共用工作樹檔案（每次皆 byte-exact 復原），但同一輪次另一個 reviewer 並行執行的 `npm test` 觀察到其中一次的中間 mutated 狀態，產生不可重現的假性失敗。
- 604b9f7 自身 round-1 review 揭露兩個缺口，本 commit 補上：scratch/temp copy 規則補上位置限定詞 `held outside the working tree under review`，避免與緊鄰的「不准寫檔案」條款被誤讀為互相矛盾；`.github/scripts/validate-structure-core.mjs` 的 `RIGOR_ANCHORS`（§5k dual-write guard）新增 `scratch/temp copy` 錨點，防止未來編輯只改動 `reviewer-common.md`／`review-packet.md` 兩份同步檔案中的一份卻不被發現。`test/validate-structure.test.mjs` 新增一筆判別測試（僅從 `review-packet.md` 移除該錨點字串，證明 §5k guard 確實會抓到這種單邊 drift）。
- `doctor.mjs` 新增選擇性加入的 `--project`／`--cwd` 旗標（規劃文件先經一輪自我審閱修正，釐清「記憶檔為空或格式錯誤時不可靜默併入令人安心的『pass, clean』、而未具名說明實際發生了什麼」與「incident journal 狀態判定應為單一規則、而非兩條各自獨立的判斷路徑」兩處歧義），疊加在既有 14 項 plugin-health 檢查之上、預設 `/ctide:doctor` 輸出逐位元組不變：新增的 `failure-memory-health` 直接重用 vigil 的 `failure-retrieve.mjs`（`resolveMemoryFile`，但排除其第三層機器全域 `~/.claude/FAILURE_MEMORY.md`、僅認專案本地兩層）與 `failure-consolidate.mjs`（`consolidationReport()`／`tagRecurrenceCandidates()`）而非重新實作；`incident-journals` 直接重用 salvage 的 incident journal schema（`INCIDENT-<YYYYMMDD>-<slug>.md` 檔名、`Status: open | mitigated | closed` 欄位）：未確認為 `closed` 者（`open`／`mitigated`／缺漏或無法解析）各自成一筆 `incident:<slug>` 項目，`open`／`mitigated` 並附上依檔名日期換算的天數。兩個新檢查皆以動態 `import()` 延遲載入 vigil 模組並包在 try/catch 中，載入失敗回報為該檢查自身的 `fail`，不會讓整個 `doctor.mjs` crash；既有的 `argument()` next-token-swallow 疑慮（新增 `KNOWN_FLAGS` guard）順帶延伸修補到既有的 `--plugin-root` 旗標本身。實作歷經多輪 vigil 修復：round 2 的 AC2 regression test 直接 mutate 真實、未隔離的 `~/.claude/FAILURE_MEMORY.md`，被同時執行的並行 review panel 兩次競爭寫入、把該機器的失敗記憶壓縮成僅剩 96 bytes 的測試 canary 字串——靠 orchestrator 重放同一 session transcript 中先前對該檔案的每一次 Read／Edit 逐步復原（公開揭露 2 處無法自動重放的殘缺，僅損及個別 entry 的「第 N 次出現」計數註記、非任何 entry 的實質內容），已記錄於 `~/.claude/FAILURE_MEMORY.md` 2026-07-23 條目；round 3 修好該測試後，另找出約 13 處遞移觸及真實路徑的站點；round 4 為 `test/doctor-project.test.mjs` 建了整檔案層級的 `before()`/`after()` HOME／USERPROFILE 隔離 hook 收斂之，其自身 review 又發現 `test/support.mjs` 模組頂層、追溯至本 repo 初始 commit 的 `globalMemExists`——在 ES module import 時期便 eager 執行 `fs.existsSync` 觸碰真實路徑，早於任何測試自身的隔離 hook 生效——是獨立於前述輪次的既存缺口；round 4 的 `arbiter` 對此發出 Stuck Summary（`NOT READY`，非再開一輪 point-patch），列出三個選項（A. 讓 `resolveMemoryFile` 不再直接呼叫裸 `os.homedir()`＋新增 repo-wide lint／CI guard 的深層結構修法；B. 使用者正式將不變量重新界定為僅限內容安全；C. 有界緩解——`globalMemExists` 改寫為惰性求值的 `hasGlobalMemory()`，加上隔離 hook 的 circuit-breaker 與既有慣例的 `try`/`catch`），使用者選擇 C，round 5 據此為 round 4 的隔離 hook 補上 circuit-breaker assertion、將 `globalMemExists` 改寫為 `hasGlobalMemory()`，並補上 `try`/`catch`，記錄於本機限定的 `.ctide/decisions/DECISION-20260724-doctor-project-memory-scope.md`（本 repo 的 `.ctide/` 全目錄皆列入 `.gitignore`，此決策記錄本身不隨 commit 出貨）。測試總數 828 → 857（823 → 850 通過、5 → 7 skip、0 fail）。
- doctor `--project` 功能上版後在 CI 的 `Validate (macos-latest)` 連續紅兩輪：第一輪（`4582bdc`）誤診為 `fs.writeFileSync`／`fs.cpSync` 寫入後立刻被子行程讀取的可見性競爭，加了 `fsyncPath`／`writeFileSyncDurable`（Windows 上 fd 需開 `"r+"` 而非 `"r"`，否則 `FlushFileBuffers` 丟 `EPERM`）並補上 `typos.toml` 的 `entrys` 白名單條目——這則修正推上去後，同一顆 commit 在真實 CI 上又原封不動紅了同樣 3 個測試，證明理論診斷錯了。重新查證後找到真正的根因：`doctor.mjs` 判斷「自己是否被直接呼叫」的邏輯拿沒經過 symlink 解析的 `process.argv[1]` 去比對會被 Node ESM loader 自動解析 symlink 的 `import.meta.url`；macOS 的 `/var` 永遠是指向 `/private/var` 的 symlink，`os.tmpdir()` 又落在其下，導致任何從暫存目錄複製再執行的腳本在 macOS 上判斷永遠是 false、整段 CLI 邏輯被靜默跳過（exit 0、無任何輸出）——先以 Windows junction 直接重現這個機制才動手修，非純猜測。repo 全庫掃描找出同樣脆弱寫法的 14 處站點（`doctor.mjs`／`map.mjs`／`ship.mjs`／vigil 全部 8 支 script／`eval/check-model-provenance.mjs`／`.github/scripts/publish-release.mjs`／`publish-release-core.mjs`，最後一處用相反方向的 `pathToFileURL` 寫法），全部改為本地 `isInvokedDirectly()`（`fs.realpathSync(process.argv[1])` 比對 `import.meta.url`，或對稱的 `pathToFileURL` 寫法），皆未新增任何 import；新增一個真的用 `fs.symlinkSync`／junction 建立實體連結來驗證的迴歸測試，不再只依賴 macOS 暫存目錄的巧合。四人 Tier-1 review panel（intent／test／code／architecture）一輪 READY，三項 major 發現（review packet 敘述不準、`publish-release.mjs`／`map.mjs` 既有測試涵蓋率是 0、14 處共用邏輯目前沒有防漂移機制）皆查證屬實但判定不阻塞、列為 fast-follow。`~/.claude/FAILURE_MEMORY.md` 的對應條目已改寫為正確根因，取代原先被推翻的可見性競爭理論。測試總數 857 → 858（850 → 851 通過、7 skip、0 fail），`npm test`／`npm run validate` 皆過；macOS CI 實際重跑確認 `Validate (macos-latest)`、`Typos`、`zizmor` 全綠。

## [0.5.0] - 2026-07-21

- Map 的圖形類禁令改寫：由「禁止形式」（不准窮舉呼叫圖、不准持久化圖形產物）改為守護兩條真正的不變量——Map 唯一持久輸出仍是 SYSTEM_MAP.md 內有引註的 prose；任何 claim 落地前必須開檔驗證並引註行號。同時明文開放環境中既有的結構索引（LSP、code-review-graph 等）可依既有的 Detect→Use→Else-Disclose 協議查詢作為線索來源，索引輸出永遠是 candidate、從不是 evidence。過程中修正一句與 `map.mjs` 實際行為不符的契約敘述：原稿聲稱索引「從不被 `map verify` 檢查」，但樹內索引目錄實際會被既有的 working-tree fingerprint 掃描、可能觸發 drift——已改為據實陳述（verify 對索引不特殊處理，樹內索引依一般未追蹤檔案雜湊，須自行排除在掃描樹外或預期偵測到 drift）。新增 `eval/cases/map-index-lead-not-evidence.json`，以 mutation-testing 驗證過的肯定式判定用語釘住「索引輸出是線索、絕非證據」這條行為。
- 五個測試檔（`contract-check`、`run-ledger`、`run-reconcile`、`map`、`release-publisher`）比照先前 `release-doctor` 的修法，新增共用的 `hardenGitSigning` helper 硬化 git fixture 對使用者全域簽章設定的隔離性，不影響出貨的 `cressetide/` payload。
- 三語 README 全文「說人話」改稿（37 處確認後套用）：長段落全面拆段增加呼吸感（每檔 341 → 357 行，多出的全是段落切分）；全形破折號「——」由 40+ 處降到 4 處，列點導入一律改冒號；清除翻譯腔用詞（紮根、持久化、刷新、正在運行、靜默、出貨、揭露、浮出、送達、承載等）；中文詞間半形斜線統一為全形「／」。英日版以 zh-TW 為源頭重新導出相同的段落結構與標點密度，並修正日文版兩處殘留的中文詞「紮根させ」。`contract-guard.js` 的 hook 表格欄改為白話描述（機制術語「以有效、依優先序解析後的值」改為其實際後果「連新建一個 settings 檔來翻也算」），事實零損失。六個 CI token、16 節標題 parity、所有連結／數字／指令字面值不變；`npm run validate` 與 `test/structure.test.mjs` 通過。
- 三語 README（`README.md`／`README.zh-TW.md`／`README.ja.md`）整體重構為 16 節的 skill 導向版面：簡介（intro 流程圖的學習迴圈行補上 ledger 半邊）→ What's inside（新增元件架構圖＋列點地圖）→ 30 秒理解（併入原「何時使用」與「非目標」）→ 快速開始 → 各 skill 專節（vigil 吸收原「運作方式」「11 個 subagent」與「好任務」摘要；salvage 六段粗體詳解壓為三個列點；map 與 doctor 首次獲得專節）→ 新增「學習迴圈」專節（run ledger 記錄、reconciliation 處置、`FAILURE_MEMORY`／`EXPERIENCE` 分工，連結 `run-ledger.md` 與 `experience-memory.md`）→ Hooks 與安全模型 → 參考資料區（設定參考、相容性、信任與發佈、成本、範例與證據、文件、授權）。壓縮遵循零損失原則——被縮短的內容皆有既有文件作為完整出處（`docs/task-writing-guide.md`、salvage references 等）；每檔 397 → 341 行。
- 新增 `.github/assets/flow_overview.svg`：手工撰寫的元件流程向量圖，三語 README 共用（純 `<text>` 元素、無 foreignObject、背景透明且文字皆壓在色塊上，GitHub 深淺色主題皆可讀）。以形狀＋顏色編碼節點類型並附圖例——skill 矩形、subagent 面板六角形、committed 記憶檔案文件形、本地 run ledger 圓柱、人／外部訊號膠囊、產出膠囊——並以虛線 skill boundary 容器正確呈現 risk-selected panel 與 arbiter 屬於 vigil run 的內部元件（修正先前沿用 ARCHITECTURE.md 平行節點畫法造成的誤讀）；取代原先依賴 GitHub mermaid 渲染的方案。`test/structure.test.mjs` 的三語 token 與 `##` 標題數 parity 檢查、`.github/scripts/validate-structure.mjs` 的 README parity 檢查皆通過。

## [0.4.0] - 2026-07-20

- 新增 `cressetide/skills/vigil/scripts/run-ledger.mjs`：在 `.ctide/ledger/` 建立 append-only 的 `runs.jsonl` 事件紀錄檔（首次寫入時同步建立巢狀 `.ctide/ledger/.gitignore`，沿用 `.ctide/output/.gitignore` 既有的 `*` 加 `!.gitignore` 寫法），做為 committed semantic state（`map/`、`memory/`、`design/`、`incidents/`）與 untracked per-run scratch（`output/`）之外的第三種狀態類別——untracked-but-persistent 的 episodic state，跨執行留存、從不覆寫或截斷。`buildRunRecord` 產生的每筆 `run` 紀錄中，`head`（`git rev-parse HEAD`）與 `files`（`git diff --name-only`）一律由 script 自行推導、沒有對應 CLI flag 可覆寫，`verdict`／`verify`／`panel` 則逐字複製既有的 machine sentinel（`ctide:delivery=`／`ctide:verify=`／`ctide:panel=`）——皆為 git／契約推導的事實，絕不由 agent 自行輸入；所有寫入路徑 fail-open，CLI 一律 exit 0，`readRunsLedger` 的容錯解析與 `failure-consolidate.mjs` 既有的 ledger 容錯讀取一致，供 `run-reconcile.mjs`／`run-consolidate.mjs` 共用。新增 `test/run-ledger.test.mjs`。
- 新增 `cressetide/skills/vigil/scripts/run-reconcile.mjs`：`scan` 唯讀掃描 ledger 中每筆尚未結案的 `run` 紀錄，比對其後續 commit 的變更檔案是否與該紀錄的 `files` 重疊、且該 commit 訊息是否命中 `fix`／`revert`／`hotfix`／`regress`（word-boundary、不分大小寫），據此分類為 `candidate-rework`／`needs-human-review`／`expire-candidate`／`open` 四種狀態之一，重疊但無對應關鍵字的情況一律印出 `needs human review` 而非靜默視為乾淨；`close`／`expire` 為僅追加的結案事件，`close` 需要合法的 `--ref` 與 `escaped`／`survived`／`superseded`／`building-upon` 四選一的 `--as`，結案理由一律由呼叫的 agent 提供，script 本身從不臆測結案原因，`expire` 僅對過了觀察窗且完全無重疊的情況自動結案為 `survived`。已接入 `navigator.agent.md` 新增的第 6 項 deliverable（高風險路徑，Stage A 內做 disposition、但從不寫入 `close`／`expire`）與 `references/plan-grounding.md`（Stage A 輸出欄位＋routing-table 新增一列）、`SKILL.md` lifecycle step 2（低／中風險路徑，main thread 直接掃描並 inline disposition）、`vigil-map-overlay.md`（同步補一句對應敘述）；`close`／`expire` 僅由 main thread 呼叫，與既有 failure-memory 的單一寫入者原則一致。新增 `test/run-reconcile.test.mjs`，含 `scan` 唯讀性的 byte-comparison 驗證。
- 新增 `cressetide/skills/vigil/scripts/run-consolidate.mjs`：`consolidateState` 依需求即時計算 ledger 的未結案數、依 `close` 事件的 `as` 分類統計，以及 14 天／≥3 次 `escaped` 結案的 tripwire（少於 3 筆歷史 `close` 事件時回報 `insufficientHistory`、絕不假造比率），本身不落地任何欄位。已接入 `SKILL.md` lifecycle step 9，且明確排在 `arbiter` verdict 鎖定之後才執行；輸出僅透過 `references/final-report.md` compact 與 `--report full` 兩種樣板新增的一行 `Ledger:` bullet（例如 `Ledger: 12 open · 1 escaped/14d`，跨過門檻才加註 `— retro suggested`，ledger 全新或無事可報時整行省略）轉述給使用者，明確排除回饋進當次執行自身的 scope／risk tier／panel 選擇。新增 `test/run-consolidate.test.mjs`。
- `references/final-report.md` 新增獨立、純敘事的 `Plan drift` 欄位（compact 與 `--report full` 皆有）：對照計畫中的路徑／風險與實際結果（契約與 git 推導、非 agent 自行宣稱）、觀察到的修復迭代次數，以及執行中發現的 Map 修正，沒有漂移時整行省略，不以分數或比率表示——是既有失敗路徑 drift-aware Stuck Summary（0.3.0，`references/verification-gate.md`）在成功路徑上的對應設計。
- 新增技術文件 `cressetide/skills/vigil/references/run-ledger.md`（`run` 與 `close` 兩種紀錄的欄位 schema、reconciliation 生命週期，以及「僅能在 verdict 鎖定後轉述給使用者、絕不回饋進當次決策」的邊界說明）與 `cressetide/skills/vigil/references/experience-memory.md`（新的 `.ctide/memory/EXPERIENCE.md` 契約：`candidate`→`validated`→`standard`→`retired` 生命週期，晉升至 `standard` 必須附帶可執行資產如測試、validator 或範本，此規則本版僅為人工判讀的 prose contract、刻意不做機器 lint——避免重蹈 `~/.claude/FAILURE_MEMORY.md` 2026-07-16 條目記錄的 Ops-Profile trust-marker 四輪未收斂 heuristic 陷阱；並明確聲明 `load-failure-memory.js` 維持只讀取 `FAILURE_MEMORY.md` 路徑、從不自動注入 `EXPERIENCE.md`）；`SKILL.md` 的 Reference Loading 清單同步新增這兩份文件的載入時機。
- 新增 `docs/benchmark-contract.md`：訂定分數／通過率僅允許出現在封閉世界（frozen fixtures、hidden acceptance checks、version-A-vs-B 比較）情境，精確引用既有的 `eval/cases/*.json`＋`eval/run-eval.mjs`（CI 內、逐字比對 `requiredTerms`／`forbiddenTerms`，非語意評分）與 `eval/manifest.yaml`＋fixtures＋`eval/baseline.md`（依需求執行、LLM 評審的 reviewer prompt-drift 套件，`eval/README.md` 已自陳非 real-world benchmark），並聲明 live 執行的成品（final report、run ledger、`run-consolidate.mjs`）從不攜帶分數；`docs/evaluation.md` 新增一段互相參照。新增 `docs/advanced/retro-practice.md`：定義 retro 以 ledger 中已結案的個案敘事為起點（cases first）、任何數字必須與其對應的配對數字同時出現（paired-count discipline，例如乾淨數絕不脫離 escaped 數單獨呈現），且 retro 產生的政策調整一律透過正常的 plan → approval → implementation → review vigil 執行落地，而非直接手動修改 skill／reference 檔案。
- 修正 `EVIDENCE.md` 殘留的版號參照（0.1.0 → 0.3.1），並新增 `## Real-world runs` section——此 section 先前僅被 `references/final-report.md` 的 Evidence Record 提及、卻從未真正建立。
- `docs/runtime-contract.md`、`README.md`／`README.zh-TW.md`／`README.ja.md`、`ARCHITECTURE.md` 同步更新 `.ctide/` 狀態樹說明：新增 `ledger/runs.jsonl` 做為第三種狀態類別（untracked-but-persistent、自帶 gitignore、跨執行留存、從不覆寫或截斷，有別於 committed 的 semantic state 與 per-run 的 scratch），並在 `memory/` 加入 `EXPERIENCE.md`。
- 全 plugin 散文層「零規則損失」壓縮（6 批次 A–F，涵蓋 vigil references、agents、salvage／map SKILL+references、doctor diagnostic-contract 共 27 個目標檔）：每批依「逐條規則盤點 → 重排去重 → 逐條處置表（零 DROPPED）→ 獨立盲測重萃取稽核 → validate／test／eval 機器閘 → reviewer 面板 → arbiter READY」把關後才進入下一批；21 檔實際變更、6 檔（salvage/SKILL.md、map/SKILL.md、wartime.md、expand-migrate-contract.md、implementer.agent.md、ui-ux-reviewer.agent.md）經逐條評估後刻意原樣交付（pin 密度或每句皆規則——零損失優先於位元組），合計 285,252 → 281,529 bytes（−3,723，−1.3%）。唯一的跨檔結構性變更：一次性 failure-memory 遷移程序由 4 份全文複本收斂為 `references/verification-gate.md` 單一 shared owner（*One-time migration procedure (shared)*）加 4 個參數化 invocation（verification-gate ×2、design-spec、operational-readiness），owner 步驟以名稱＋編號綁定；同場修正 `reviewer-selection.md`／`review-packet.md` 兩處自始不存在的懸空引用（*Recall vs precision* → 實際標題 *Multi-lens review rationale*）。frozen 區（review-packet 的 Shared reviewer contract 區塊 ↔ `reviewer-common.md`）與 `vigil-map-overlay.md` 依 plan-gate 決議整段排除未動；全部機器 pin（validator §5b/5d/5e/5f/5j/5k/9a/9c/9e/9f、workflow-contracts 行錨、trust-marker exactly-once）逐批 grep 重驗並由 723 測試基線全程守住。
- `vigil/SKILL.md` 結構性壓縮（progressive disclosure）：29341 → 23611 bytes（-19.5%）。逐條規則盤點後僅做搬移與去重、未刪除任何 normative rule——Language And Text Integrity 全文移至新增的 `references/language-integrity.md`（SKILL.md 保留使用者語言／機器檢查 token 清單的 stub）、Reference Loading 每行縮為載入時機觸發句、Plan Gate 的 Bash tripwire 列舉改指向 `plan-gate.js` 自身註解、Core Rules 與 lifecycle 間的重複（best-practices、cost 規則）各收斂為單一完整版本、其餘 lifecycle 細節（plan-grounding 機制、design.md／task contract schema、瀏覽器證據欄位、evidence-substitution 條件）改為一行觸發加既有 reference 指標；zh-TW 尾段末條同步改寫：lifecycle 內容由 Cressetide 自身維護，不再對應任何上游（udflow）映射。機器 gate 全數保留（§5f 九個 literal、§9a reference 覆蓋、11 個 subagent roster 句、workflow-contracts 錨點、`${CLAUDE_PLUGIN_ROOT}` 前綴）。

## [0.3.1] - 2026-07-19

- 新增共用的 `normalizeGzipOsByte` helper（`publish-release-core.mjs`，由 `publish-release.mjs` re-export），在寫入磁碟或計算 checksum 前將 gzip 輸出的 RFC 1952 OS 欄位（offset 9）正規化為 `0xFF`：修正 Node `zlib.gzipSync()` 依建置平台設定不同 OS byte（本機 Windows 為 `0x0a`、CI 的 ubuntu-latest 為 `0x03`），導致同一 source input 在不同平台建置出不同 SHA-256 release archive 的 determinism 缺陷；`createDeterministicPluginArchive` 與 `createArchive` 兩個既有呼叫點皆已套用，`test/release-doctor.test.mjs`、`test/release-publisher.test.mjs` 既有的 determinism 測試同步新增 offset 9 斷言。
- Map 的進入點候選（`map.mjs` 的 `entries`，餵給 Architecture boundaries 與 Execution flows 的 Candidate entry points）新增檔名慣例（`main`／`index`／`app`／`program`／`server`）以外的兩個訊號：檔案開頭 2 bytes 為 shebang（透過既有的 `assertContainedPath` 做 bounded prefix read，containment violation fail-closed、一般 I/O 錯誤 fail-open），以及 package.json 正規化後的 `bin` 欄位且必須存在於已走訪的 `files` 集合中才算數（避免不存在的路徑被引用為證據）；三個訊號 union 後去重排序。`packageFacts()` 新增 `bin: string[]` 欄位（字串、物件、其餘型別分別正規化為單一元素陣列、`Object.values(...)`、`[]`，並正規化路徑分隔符與開頭 `./`），`scripts`／`dependencies`／`description` 既有行為不變。新增技術文件 `cressetide/skills/map/references/evidence-discovery.md`，`navigator.agent.md`、`cartographer.agent.md` 的 Cressetide Map 擴充章節同步指向它。

## [0.3.0] - 2026-07-18

- 最終報告新增獨立、條件式的 `Live-verification gap` 欄位（`references/final-report.md`，compact 與 `--report full` 皆有）：當 UI 或可觀察執行行為的驗收條件因 Detect→Use→Else-Disclose 找不到 live-verification 能力（瀏覽器或 `app-launch.md` 涵蓋的非 UI live process）時，明確具名揭露，不再淹沒在泛用的 External capabilities 欄位裡；`arbiter.agent.md` 同步新增對應規則，涵蓋 UI 與非 UI 兩種情境。
- `arbiter` 的修復迴圈卡住判斷（連續 2 次同類 blocker → Stuck Summary）新增 drift 判斷：沿用既有的 `contract-check.mjs` scope-diff 與 bidirectional traceability 證據，揭露這次修復是否仍在收斂到原始 acceptance criteria、或已經漂移——純揭露性質，既有的連續 2 次觸發門檻不變；`verification-gate.md`、`SKILL.md`、`vigil-map-overlay.md` 同步更新以保持一致。
- 新增 `references/test-layer-boundaries.md`：定義 unit/integration/E2E 三層的責任邊界、mocking boundary 與判斷法則，並涵蓋 Python、TypeScript/JavaScript 兩種生態系的具體慣例；由 `SKILL.md` 的 Reference Loading 清單與 `test-reviewer.agent.md` 引用。
- 修正 `doctor/SKILL.md`、`docs/command-reference.md`、`docs/compatibility.md`、`map/SKILL.md`、`map/references/system-map-contract.md`、`docs/map-contract.md`、`docs/runtime-contract.md` 共 7 處殘留自 0.1.0、在 0.2.0 bump 時未同步更新的版號字串；目前沒有 CI guard 涵蓋此類散文版號一致性。

## [0.2.0] - 2026-07-16

- Salvage 的 wartime 流程新增對 Map canonical section 的引用：Stage 1 的 Blast radius 引用 `Architecture boundaries` 與 `Execution flows`，入侵研判問題引用 `Data flows` 的 Trust boundary 欄，Stage 4 diagnose 開場先查閱 `Risk and uncertainty` 已記錄的高風險模組、破壞性路徑與 production-only 假設，再進行故障分類。
- Map 新增 canonical 的 `System overview` section，產生於 SYSTEM_MAP.md 最前面（`Repository structure` 之前），並吸收原本僅屬於 Ops Profile 的 System overview 內容，改為一行描述加上指向 `Architecture boundaries`（元件與進入點）與 `External integrations`（外部相依）的導覽，避免同一事實在兩處維護。
- `map verify` 新增對 Ops Profile 其餘 9 個欄位（Access inventory、Rollback、Feature flags & kill switches、Backups、Breach readiness、Observability inventory、Run in isolation、External dependencies、Approvals map）的檢查：缺漏欄位、仍停留在 heuristic placeholder、信任標記格式錯誤、或已填寫內容卻缺少對應機器可讀信任標記（`missing trust tag`）都會回報對應的 finding；`map create`/`map refresh` 同步產生這 9 個欄位的預設骨架，並與其餘 canonical section 一樣在每次 refresh 時重新產生。

## [0.1.1] - 2026-07-16

- 將 CI 工作流程中的 `actions/setup-node` 從 6.4.0 升級到 7.0.0（Dependabot）。
- 修正 `release setup-node` 快取合約的測試，改以比對任一 40 碼 hex SHA 取代寫死單一版本的 SHA，避免每次 action 版本更新都導致驗證失敗。

## [0.1.0] - 2026-07-15

- 發布 Cressetide 的 `vigil`、`salvage`、`map`、`doctor` 四個公開 Skill 與十一個 agent roster。
- 提供 plan gate、task contract、驗證、風險式 review panel、incident response、system map 與本機 Doctor 流程。
- 加入六個 fail-open hooks、`.ctide/` 專案狀態契約、結構與文字完整性驗證、測試及 eval。
- 強化 immutable tag、deterministic archive、exact asset inventory、checksum 與 provenance attestation 的 release 流程。
